.container
  #badges_carousel.carousel.slide{"data-ride" => "carousel", "data-interval" => "false"}
    .carousel-inner
      - @weeks.each_with_index do |week, i|
        .item{class: ("active" unless i != 0)}
          .row
            .col-md-12
              %h1.text-center
                = week.name
              .row.text-center
                - week.days.each do |day|
                  .day_box
                    - if day.active?
                      = image_tag day.image.url
                    - elsif day.blocked?
                      = image_tag day.image.url
                      .day_overlay
                        = image_tag "locked.png"
                    - elsif day.done?
                      = image_tag day.image.url
                      .day_overlay
                        = image_tag "fullfilled.png"
                    - else
                      = image_tag "0.png"
    #breadcrumb
      %a.next-prev{"data-target" => "#badges_carousel", "data-slide" => "prev"}
        .arrow-left
      %ol.carousel-indicators
        %li{"data-target" => "#badges_carousel", "data-slide-to" => "0"}
      %a.next-prev{"data-target" => "#badges_carousel", "data-slide" => "next"}
        .arrow-left